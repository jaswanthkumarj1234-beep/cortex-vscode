# Install Cortex MCP

Cortex uses the **MCP protocol** (Model Context Protocol) to communicate with your AI coding assistant.

## What gets installed?

```bash
npm install -g cortex-mcp
```

This installs:
- 🧠 **cortex-mcp** — The memory server (runs via stdio)
- 📦 **SQLite database** — Local, encrypted memory storage
- 🔍 **Embedding engine** — Semantic search over your memories

## System Requirements
- Node.js >= 18
- ~50MB disk space

The setup wizard will detect your environment and install everything automatically.
